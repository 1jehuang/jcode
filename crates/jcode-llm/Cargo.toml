[package]
name = "jcode-llm"
version = "0.1.0"
edition = "2024"
description = "LLM Provider Integration - Support for Deepseek, vLLM, llama.cpp"

[dependencies]
# Async runtime
tokio = { workspace = true, features = ["full"] }
async-trait = { workspace = true }
reqwest = { version = "0.12", features = ["json", "stream"] }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# Error handling
anyhow = { workspace = true }
thiserror = { workspace = true }

# Logging
tracing = { workspace = true }

# Time
chrono = { workspace = true }

# UUID
uuid = { workspace = true, features = ["v4"] }

# Futures & Streaming
futures = { workspace = true }
tokio-stream = { version = "0.1", features = ["sync", "io-util"] }

# HTTP server (for REST API - OpenAI compatible)
axum = { version = "0.8", features = ["ws", "macros", "json"] }
tower-http = { version = "0.6", features = ["cors", "trace"] }
serde_with = "3"

[dev-dependencies]
tracing-subscriber = "0.3"
