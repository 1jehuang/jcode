[package]
name = "jcode-core-types"
version = "0.1.0"
edition = "2024"
description = "Core type definitions: config, workspace, ambient, auth, gateway"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
