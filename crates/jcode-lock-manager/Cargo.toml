[package]
name = "jcode-lock-manager"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.0", features = ["sync", "time", "rt", "macros", "test-util"] }

thiserror = "1.0"
tracing = "0.1"

[features]
default = []
deadlock-detection = ["tokio/time"]
